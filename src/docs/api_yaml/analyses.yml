summary: Get all analyses
tags:
  - API
description: ""
parameters:
  - in: body
    name: body
    schema:
      type: object
      properties:
        token:
          type: string
responses:
  200:
    description: OK.
    schema:
      type: array
      items:
        $ref: '#/definitions/Analysis'